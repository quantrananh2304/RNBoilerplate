import {
  fetchAllProductsByCategory,
  fetchAllCategories,
} from './fakestoreapiservice';

const api = {
  fakeStoreApi: {
    fetchAllProductsByCategory,
    fetchAllCategories,
  },
};

export default api;
